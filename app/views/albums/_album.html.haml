.album{id: "album_#{album.id}", class: "album_#{album.name}"}
  %h3= album.name
  -if(album.artist)
    %h4=album.artist.name
  .inline-links
    -if(current_user.favorite?(album))
      = link_to 'Remove From Favorites',album_favorite_path(album, current_user.favorite(album)), method: :delete, class: "btn btn-primary btn-sm"
    -else
      = link_to 'Add to Favorites', album_favorites_path(album), method: :post, class: "btn btn-primary btn-sm"

    = link_to 'Show Album', collection_path(album.my_collection.id), id: "Show Album", class: "btn btn-primary btn-sm"

    = link_to 'Edit Album', edit_collection_path(album.my_collection.id), id:"Edit Album", class: "btn btn-primary btn-sm"

    = link_to 'Destroy Album', album.my_collection, id: "Destroy Album",:method => :delete, :data => { :confirm => 'Are you sure?' }, remote: true, class: "btn btn-primary btn-sm"